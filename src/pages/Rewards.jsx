/**
 * Página de recompensas de la aplicación.
 *
 * Componente básico que identifica la ruta `/recompensas` y mantiene el
 * estilo oscuro de la app.
 */
export default function Rewards() {
  return (
    <main className="flex min-h-[calc(100vh-13rem)] items-center justify-center bg-[#0F172A] px-4 text-white">
      <p className="text-center text-lg font-semibold">Pantalla de Recompensas</p>
    </main>
  )
}
