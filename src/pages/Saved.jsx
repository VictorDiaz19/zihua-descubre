/**
 * Página de guardados de la aplicación.
 *
 * Componente básico que identifica la ruta `/guardados` y muestra un texto
 * centrado dentro del estilo oscuro de la app.
 */
export default function Saved() {
  return (
    <main className="flex min-h-[calc(100vh-13rem)] items-center justify-center bg-[#0F172A] px-4 text-white">
      <p className="text-center text-lg font-semibold">Pantalla de Guardados</p>
    </main>
  )
}
