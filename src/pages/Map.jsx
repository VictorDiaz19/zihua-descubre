/**
 * Página de mapa de la aplicación.
 *
 * Componente básico que identifica la ruta `/mapa` y mantiene el estilo
 * oscuro del proyecto.
 */
export default function Map() {
  return (
    <main className="flex min-h-[calc(100vh-13rem)] items-center justify-center bg-[#0F172A] px-4 text-white">
      <p className="text-center text-lg font-semibold">Pantalla de Mapa</p>
    </main>
  )
}
